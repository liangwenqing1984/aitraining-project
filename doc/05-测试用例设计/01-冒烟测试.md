# 冒烟测试

> 目的：验证系统核心链路可用，每次部署后必须通过。

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| ST-01 | 后端服务启动 | Node.js 18+，数据库可连接 | 执行 `cd code/backend && npm run dev` | 控制台输出"PostgreSQL数据库表初始化完成"，端口 3004 监听 |
| ST-02 | 前端服务启动 | 后端已启动 | 执行 `cd code/frontend && npm run dev` | Vite 启动成功，端口 3000，无编译错误 |
| ST-03 | 数据库连接 | PostgreSQL 10.1.1.113:7300 可访问 | 后端启动时自动执行 initDatabase() | 6 张表全部创建/确认，无报错 |
| ST-04 | 页面访问 | 前后端均已启动 | 浏览器访问 `http://localhost:3000` | 重定向到登录页，无白屏/JS 报错 |
| ST-05 | OAuth2 登录 | Auth 服务可访问 | 点击登录 → 输入账号密码 → 授权 | 成功跳回系统主页，侧边栏显示用户信息 |
| ST-06 | 任务 CRUD | 已登录 | 进入数据采集页 → 创建任务 | 任务创建成功，显示在任务列表中 |
| ST-07 | 文件下载 | 存在已完成的采集任务 | 进入文件管理页 → 点击下载 | 浏览器触发 Excel 文件下载 |
| ST-08 | AI 配置 CRUD | 已登录 | 进入 AI 配置页 → 添加模型配置 → 测试连接 | 配置保存成功，测试连接返回模型信息 |
| ST-09 | 页面切换 | 已登录 | 依次点击侧边栏各菜单项 | 每个页面正常渲染，无报错，无白屏 |
