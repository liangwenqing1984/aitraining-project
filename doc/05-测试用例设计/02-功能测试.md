# 功能测试

> 目的：验证各功能模块的业务逻辑正确性。

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| FT-01 | 创建单关键词任务 | 已登录 | 选择"智联招聘"→ 输入"Java开发" → 添加 → 选择"北京" → 设置页数 2 → 创建 | 任务创建成功，status=pending，config 包含关键词和城市 |
| FT-02 | 批量创建任务 | 已登录 | 输入"Java开发,Python开发" → 选择"北京,上海" → 预览组合 → 确认创建 | 生成 2×2=4 个任务，每个任务名称含关键词+城市 |
| FT-03 | 启动采集任务 | 存在 pending 任务 | 点击任务行的"启动"按钮 | 状态变为 running，监控页出现进度条和日志 |
| FT-04 | 任务进度推送 | 任务 running | 打开任务监控页 → 观察进度条 | WebSocket 实时更新 current/total/progress，日志分级彩色输出 |
| FT-05 | 断点续传 | 存在已部分采集的任务（有 Excel 文件） | 停止任务 → 重新启动 | 日志显示"从断点恢复，已采集 N 条"，跳过已有 jobId |
| FT-06 | 暂停与恢复 | 任务 running | 点击"暂停" → 等待状态变为 paused → 点击"恢复" | 暂停后 current 不变，恢复后继续增长 |
| FT-07 | 停止任务 | 任务 running | 点击"停止" | 浏览器关闭，状态变为 stopped/failed |
| FT-08 | 任务完成 | 任务 running，页数设置合理 | 等待采集完成 | 状态变为 completed，record_count > 0，生成 Excel 文件 |
| FT-09 | AI 数据增强 | 任务 completed，有 Excel 数据 | 点击任务行的"AI 增强"按钮 | WebSocket 推送增强进度，完成后 enrichment:completed 事件触发 |
| FT-10 | 增强结果验证 | 增强已完成 | 进入智能分析页 → 选择对应文件 | 薪资分布/职位分类/学历/技能/行业等图表正常展示 |
| FT-11 | AI 深度分析 | 增强数据存在 | 智能分析页 → 点击"AI 深度分析" | 20-40 秒后生成报告（摘要 + 多 sections + ECharts 图表） |
| FT-12 | 报告历史查看 | 已有至少 1 份报告 | 智能分析页 → 查看报告历史下拉 | 显示所有历史报告，点击可切换查看 |
| FT-13 | 自然语言查询 | 增强数据存在 | 智能查询页 → 输入"薪资最高的 10 个岗位" → 回车 | 返回数据表格 + 自然语言总结 + 生成的 SQL |
| FT-14 | 快捷提问 | 已加载智能查询页 | 点击快捷标签"各城市的岗位数量分布" | 自动执行查询，返回城市分布数据 |
| FT-15 | 查询历史 | 已执行过查询 | 点击左侧历史记录条目 | 右侧对话区加载该条查询的完整结果 |
| FT-16 | 任务筛选查询 | 存在多个任务的增强数据 | 智能查询页 → 顶部下拉框选择一个任务 → 输入查询 | 生成的 SQL 包含 `WHERE task_id = 'xxx'` 限定条件 |
| FT-17 | LLM 配置管理 | 已登录 | AI 配置页 → 添加 DeepSeek 配置 → 填入 API Key → 勾选任务路由 → 测试连接 | 配置保存，测试返回模型名称和延迟 |
| FT-18 | 文件管理 | 存在多个 Excel 文件 | 文件管理页 → 查看列表 → 点击预览 | 文件列表正确显示，预览弹窗展示 Excel 前 20 行数据 |
| FT-19 | 文档页面 | 已登录 | 侧边栏 → 项目文档 | 左侧层级菜单展开/折叠正常，右侧内容点击即时渲染 |
| FT-20 | 职位向量化索引 | 任务已完成且有增强数据 | 语义搜索页 → 点击"开始索引" | 索引完成，显示成功数量，sp_job_embeddings 有数据 |
| FT-21 | 语义搜索查询 | 已有向量索引 | 输入"远程后端开发" → 搜索 | 返回相似度排序的职位列表，含匹配分数 |
| FT-22 | 简历文件上传匹配 | 已有向量索引 | 上传 PDF/DOCX/TXT 简历文件 | 提取技能/经验 → 匹配并返回职位列表 |
| FT-23 | 简历文本粘贴匹配 | 已有向量索引 | 粘贴简历文本 → 匹配 | 同上传流程，返回匹配结果 |
| FT-24 | AI 问答-发送消息 | 文档索引已完成 | AI 问答页 → 输入问题 → 发送 | 返回带来源引用的回答 |
| FT-25 | AI 问答-多轮对话 | 同上 | 连续发送多个相关问题 | 保持上下文连贯，后续回答参考前文 |
| FT-26 | AI 问答-会话管理 | 已有多个会话 | 查看会话列表 → 切换会话 | 会话列表正确，切换后加载对应消息历史 |
| FT-27 | 全量文档索引 | docs/ 目录有 .md 文件 | 文本向量管理页 → 全量文档索引 | 扫描并索引所有文档，显示分块数 |
| FT-28 | 文件上传索引 | 已登录 | 选择 .md/.txt 文件 → 上传 | 文件分块并向量化，存入 sp_doc_embeddings |
| FT-29 | 多文件上传索引 | 已登录 | 选择 3 个文件 → 上传 | 全部成功处理，显示成功/失败统计 |
| FT-30 | 索引记录管理 | 已有索引数据 | 查看分页列表 → 按来源删除 | 列表正确分页，删除后记录移除 |
| FT-31 | 训练数据集构建 | 增强数据存在 | 模型训练页 → 构建数据集 | 生成正/负样本 JSONL 文件，显示样本数 |
| FT-32 | 训练任务启动 | 数据集已构建 | 配置超参数 → 启动训练 | Python 训练脚本启动，WebSocket 推送进度 |
| FT-33 | 模型部署 | 训练已完成 | 模型列表 → 部署到 Ollama | Ollama 模型列表中出现新模型 |
| FT-34 | 数据看板概览 | 系统中有任务数据 | 进入数据看板 | 指标卡片/趋势图/覆盖率正常展示 |
| FT-35 | 角色管理 CRUD | 已登录 admin | 系统管理 → 角色管理 → 创建/编辑/删除 | CRUD 操作正常 |
| FT-36 | 权限管理 CRUD | 已登录 admin | 系统管理 → 权限管理 → 添加权限 | resource:action 格式权限创建成功 |
| FT-37 | 菜单管理 | 已登录 admin | 系统管理 → 菜单管理 → 调整排序/增删 | 树形菜单操作正常 |
| FT-38 | 用户角色分配 | 已有角色和用户 | 用户管理 → 分配角色 | 角色分配成功，刷新后生效 |
